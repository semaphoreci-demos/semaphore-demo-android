version: v1.0
name: Deploy Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
  containers:
    - name: main
      image: semaphoreci/android:29

blocks:
  - name: 'Test'
    task:
      jobs:
        - name: 'test job'
          commands:
            - echo "deploy"
